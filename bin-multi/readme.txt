This directory contains a "multi-session" version of the plugin.

Multi-session means the program stack will be performed by enstabilish a
connection on every part of it.

This method is not used by this plug-in because is slower than the mono-session
version. The files are kept for future reference. 
